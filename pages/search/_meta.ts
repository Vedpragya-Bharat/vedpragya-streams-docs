import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  index: 'Overview',
  parameters: 'Query parameters',
  response: 'Response shape',
  'live-prices': 'Enriching results with live prices',
};

export default meta;
