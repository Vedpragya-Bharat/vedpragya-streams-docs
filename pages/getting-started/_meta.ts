import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  index: 'Overview',
  authentication: 'Authentication',
  'first-request': 'Your first request',
  environments: 'Environments & base URLs',
};

export default meta;
