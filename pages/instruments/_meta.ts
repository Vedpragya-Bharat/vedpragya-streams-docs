import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  index: 'Overview',
  'ltp-snapshot': 'LTP snapshot',
  ohlcv: 'OHLCV & history',
  'canonical-format': 'Canonical instrument format',
};

export default meta;
